name: animated_splash_themes
description: >-
  Animated Flutter splash screens with 5 built-in styles (Particles, Neon, Grid,
  Bounce, Expand): random selection, custom colors, configurable duration.
version: 1.1.4
repository: https://github.com/piro-hiroki/animated_splash_themes
issue_tracker: https://github.com/piro-hiroki/animated_splash_themes/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
